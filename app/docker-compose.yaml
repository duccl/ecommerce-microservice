version: "3.0"
services:
  catalogdb:
    image: mongo
    restart: always
    expose:
      - 27017
  catalog:
    build: ./src/catalogs.api
    depends_on:
      - catalogdb
    ports:
      - "8080:80" 